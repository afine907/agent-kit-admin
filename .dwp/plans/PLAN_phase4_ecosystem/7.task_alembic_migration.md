# Task 7: Alembic migration for new tables

## 1. Context

Task 2 introduces a `webhooks` table. Need a proper Alembic migration.

**Files to create:**
- `apps/server/alembic/versions/` — new migration file

## 2. Goal

Clean Alembic migration for the webhooks table.

## 3. Instructions

1. Generate migration: `cd apps/server && alembic revision --autogenerate -m "add webhooks table"`
2. Verify columns: id (UUID PK), team_id (FK to teams), url (TEXT), secret (VARCHAR), events (JSONB), active (BOOLEAN default true), created_at, updated_at
3. Add index on team_id
4. Test upgrade and downgrade

## 4. Acceptance Criteria

- `alembic upgrade head` succeeds
- `alembic downgrade -1` succeeds cleanly
- Table structure matches spec
- Index on team_id exists

## 5. Validation

```bash
cd apps/server && alembic upgrade head && alembic downgrade -1 && alembic upgrade head
```

## 6. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Generate migration.
- [ ] 3. Verify migration content.
- [ ] 4. Test upgrade/downgrade.
- [ ] 5. Update plan README `[x]`.
- [ ] 6. Update PROGRESS.md.
- [ ] 7. Commit: `git commit -m "chore(server): alembic migration for webhooks table - Task 7 of PLAN_phase4_ecosystem"`
- [ ] 8. Update Log below.

## 7. Completion & Log

- **Status:** pending
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Validation results:**
- **Notes / follow-ups:**
