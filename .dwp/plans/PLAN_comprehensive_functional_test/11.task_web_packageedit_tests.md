# Task 11: Web PackageEdit 完善测试

## 1. Context

- 影响范围：`apps/web/src/components/PackageEdit.tsx`
- 测试框架：Vitest + @testing-library/react
- v0.2.0 新增 license/repository/homepage 字段

## 2. Goal

为 PackageEdit 组件的新字段编写测试。

## 3. Instructions

> **Before starting:** Re-read the plan README section 1 (Goal) to ensure this task's work aligns with the overall objective.

### 测试用例

1. **license 字段渲染** — 验证 license 输入框存在
2. **repository 字段渲染** — 验证 repository URL 输入框存在
3. **homepage 字段渲染** — 验证 homepage URL 输入框存在
4. **字段编辑保存** — 修改字段后保存验证 API 调用包含新字段
5. **i18n 标签** — 验证所有标签使用 i18n 翻译
6. **已有值回显** — 加载已有数据时验证字段正确回显

### 实现方式

- 扩展 `apps/web/src/components/__tests__/PackageEdit.test.tsx`

## 4. Acceptance Criteria

- [ ] 覆盖 ≥6 个测试用例
- [ ] 所有测试通过
- [ ] 测试覆盖新字段的渲染和保存

## 5. Validation

```bash
pnpm --filter agent-kit-web lint && pnpm --filter agent-kit-web typecheck && pnpm --filter agent-kit-web test
```

## 6. Execution Checklist

- [ ] 阅读 PackageEdit.tsx 源码
- [ ] 扩展 PackageEdit.test.tsx
- [ ] 运行测试确保全部通过
- [ ] 运行 lint + typecheck 确保无错误

## 7. Completion & Log

(To be filled after completion)
