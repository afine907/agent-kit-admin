# Task 9: Web TeamPackagesTab 按钮测试

## 1. Context

- 影响范围：`apps/web/src/components/TeamPackagesTab.tsx`
- 测试框架：Vitest + @testing-library/react
- v0.2.0 修复了 Publish 和 Delete 按钮的行为

## 2. Goal

为 TeamPackagesTab 的 Publish 和 Delete 按钮行为编写测试。

## 3. Instructions

> **Before starting:** Re-read the plan README section 1 (Goal) to ensure this task's work aligns with the overall objective.

### 测试用例

1. **Publish 按钮导航** — 点击 Publish 验证导航到发布页面
2. **Delete 按钮显示确认框** — 点击 Delete 验证确认对话框出现
3. **确认删除调用 API** — 点击确认后验证 API 调用
4. **取消删除不调用 API** — 点击取消后验证 API 未被调用
5. **删除成功后刷新列表** — 删除成功后验证列表更新
6. **i18n 标签** — 验证按钮文本使用 i18n 翻译

### Mock 策略

- Mock `useNavigate` 验证导航
- Mock API 调用验证 delete 请求
- Mock `useTranslation` 验证 i18n

## 4. Acceptance Criteria

- [ ] 覆盖 ≥6 个测试用例
- [ ] 所有测试通过
- [ ] 测试覆盖按钮交互和 API 调用

## 5. Validation

```bash
pnpm --filter agent-kit-web lint && pnpm --filter agent-kit-web typecheck && pnpm --filter agent-kit-web test
```

## 6. Execution Checklist

- [ ] 阅读 TeamPackagesTab.tsx 源码
- [ ] 创建测试文件
- [ ] 运行测试确保全部通过
- [ ] 运行 lint + typecheck 确保无错误

## 7. Completion & Log

(To be filled after completion)
