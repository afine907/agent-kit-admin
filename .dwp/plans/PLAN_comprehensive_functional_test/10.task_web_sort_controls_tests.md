# Task 10: Web 排序控件测试

## 1. Context

- 影响范围：`apps/web/src/pages/Home.tsx`, `apps/web/src/hooks/usePackages.ts`
- 测试框架：Vitest + @testing-library/react
- v0.2.0 新增排序下拉菜单（newest, downloads, name）

## 2. Goal

为 Home 页面的排序控件编写测试。

## 3. Instructions

> **Before starting:** Re-read the plan README section 1 (Goal) to ensure this task's work aligns with the overall objective.

### 测试用例

1. **排序下拉渲染** — 验证排序下拉菜单存在且有三个选项
2. **选择 newest** — 选择 newest 排序后验证数据按创建时间降序
3. **选择 downloads** — 选择 downloads 排序后验证数据按下载量降序
4. **选择 name** — 选择 name 排序后验证数据按名称升序
5. **排序切换** — 切换排序后验证数据重新排列
6. **默认排序** — 验证默认排序方式

### Mock 策略

- Mock usePackages hook 返回测试数据
- Mock API 调用验证排序参数传递

## 4. Acceptance Criteria

- [ ] 覆盖 ≥5 个测试用例
- [ ] 所有测试通过
- [ ] 测试覆盖三种排序方式

## 5. Validation

```bash
pnpm --filter agent-kit-web lint && pnpm --filter agent-kit-web typecheck && pnpm --filter agent-kit-web test
```

## 6. Execution Checklist

- [ ] 阅读 Home.tsx 和 usePackages.ts 源码
- [ ] 创建或扩展 `apps/web/src/pages/__tests__/Home.test.tsx`
- [ ] 运行测试确保全部通过
- [ ] 运行 lint + typecheck 确保无错误

## 7. Completion & Log

(To be filled after completion)
