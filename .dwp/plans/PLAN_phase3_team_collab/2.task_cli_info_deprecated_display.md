# Task 2: CLI info — enhance deprecated/yanked display

## 1. Context

`apps/cli/src/commands/info.ts` already shows `[deprecated]` and `[yanked]` tags (lines 199-200) but without color coding or summary warnings.

**Files to modify:**
- `apps/cli/src/commands/info.ts` — enhance display
- `apps/cli/tests/commands/info.test.ts` — add display tests

## 2. Goal

Enhance `akit info` output with color-coded deprecated/yanked indicators and a summary warning when the latest version has issues.

## 3. Instructions

1. Read `apps/cli/src/commands/info.ts`, find version list display logic
2. Add color: deprecated versions → `chalk.yellow('[deprecated]')`, yanked → `chalk.red('[yanked]')`
3. Add summary line at top of output if latest version is deprecated or yanked:
   - `⚠️ 最新版本 X.Y.Z 已废弃` (yellow)
   - `❌ 最新版本 X.Y.Z 已撤回` (red)
4. Write tests for display logic (mock API responses)

## 4. Acceptance Criteria

- Deprecated versions shown in yellow
- Yanked versions shown in red
- Summary warning if latest version is deprecated/yanked
- Unit tests cover display logic
- Existing tests pass

## 5. Validation

```bash
cd apps/cli && pnpm test && pnpm lint && pnpm typecheck
```

## 6. Rollback

Revert `info.ts` changes.

## 7. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Read `apps/cli/src/commands/info.ts`.
- [ ] 3. Implement color-coded display.
- [ ] 4. Add summary warning logic.
- [ ] 5. Write unit tests.
- [ ] 6. Run validation.
- [ ] 7. Update plan README `[x]`.
- [ ] 8. Update PROGRESS.md.
- [ ] 9. Commit: `git commit -m "feat(cli): color-coded deprecated/yanked in info command - Task 2 of PLAN_phase3_team_collab"`
- [ ] 10. Update Log below.

## 8. Completion & Log

- **Status:** completed
- **Timestamp:** 2026-07-10T21:19+08:00
- **Summary of work:** Added color-coded deprecated (yellow) and yanked (red) display in info command. Added summary warning when latest version is deprecated/yanked.
- **Files changed:** apps/cli/src/commands/info.ts, apps/cli/tests/commands/info.test.ts
- **Validation results:** 190 tests passed, 0 lint errors
- **Notes / follow-ups:** none
