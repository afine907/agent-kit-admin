# Task 12: Structured logging

## 1. Context

Current logging may use plaintext. Need structured JSON logging.

## 2. Goal

All log output in valid JSON with request context.

## 3. Instructions

1. Configure Python logging with JSON formatter
2. Add request ID middleware (UUID per request)
3. Include: timestamp, level, message, request_id, user_id
4. Update existing log calls to use structured format
5. Log levels: DEBUG dev, INFO prod, WARNING business events

## 4. Acceptance Criteria

- All log output is valid JSON in production mode
- Request ID propagated through request lifecycle
- No plaintext log messages in production

## 5. Validation

```bash
cd apps/server && python -c "from app.core.logging import setup_logging; setup_logging(); print('OK')"
```

## 6. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Implement structured logging.
- [ ] 3. Add request ID middleware.
- [ ] 4. Update existing log calls.
- [ ] 5. Update plan README `[x]`.
- [ ] 6. Commit: `git commit -m "feat(server): structured JSON logging - Task 12 of PLAN_phase5_production"`

## 7. Completion & Log

- **Status:** pending
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Notes / follow-ups:**
