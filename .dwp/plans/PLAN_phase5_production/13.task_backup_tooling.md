# Task 13: Automated backup tooling

## 1. Context

No backup scripts exist.

## 2. Goal

Scripts for PostgreSQL and MinIO backup with rotation.

## 3. Instructions

1. `scripts/backup/backup-db.sh` — pg_dump, compress, rotate (7 daily, 4 weekly)
2. `scripts/backup/backup-files.sh` — MinIO bucket sync
3. `scripts/backup/restore-db.sh` — restore from backup
4. `scripts/backup/restore-files.sh` — restore MinIO data
5. `scripts/backup/cron-setup.sh` — example crontab
6. All scripts must be executable, with `--help` flag
7. Document in `docs/deployment/backup.md`

## 4. Acceptance Criteria

- All scripts executable and tested
- Backup rotation works
- Restore scripts verified
- Documentation complete

## 5. Validation

```bash
bash scripts/backup/backup-db.sh --help
bash scripts/backup/backup-files.sh --help
```

## 6. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Create backup scripts.
- [ ] 3. Create restore scripts.
- [ ] 4. Document.
- [ ] 5. Update plan README `[x]`.
- [ ] 6. Commit: `git commit -m "ops: backup tooling - Task 13 of PLAN_phase5_production"`

## 7. Completion & Log

- **Status:** pending
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Notes / follow-ups:**
