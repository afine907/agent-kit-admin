# Task 8: API documentation (OpenAPI)

## 1. Context

FastAPI auto-generates OpenAPI but endpoint descriptions may be incomplete.

## 2. Goal

Complete OpenAPI documentation with examples, served at `/docs` and `/redoc`.

## 3. Instructions

1. Add detailed docstrings to all API endpoints
2. Add request/response examples to Pydantic schemas
3. Generate static `docs/api/openapi.json`
4. Create `docs/api/README.md`
5. Verify `/docs` (Swagger UI) and `/redoc` work

## 4. Acceptance Criteria

- `/docs` serves Swagger UI with all endpoints
- `/redoc` serves ReDoc
- All endpoints have descriptions
- Static openapi.json generated

## 5. Validation

```bash
cd apps/server && python -c "from app.main import app; s = app.openapi(); assert len(s['paths']) > 20"
```

## 6. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Add docstrings and examples.
- [ ] 3. Generate static spec.
- [ ] 4. Update plan README `[x]`.
- [ ] 5. Commit: `git commit -m "docs: API documentation with OpenAPI - Task 8 of PLAN_phase5_production"`

## 7. Completion & Log

- **Status:** pending
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Notes / follow-ups:**
