# Task 2: Server unit test coverage improvement

## 1. Context

Server coverage needs to reach >80%. Priority: services layer (package, version, team, dependency, webhook).

## 2. Goal

Increase server test coverage to >80%.

## 3. Instructions

1. Identify untested modules from Task 1's coverage report
2. Add tests for: error paths, edge cases, permission checks, validation failures
3. Priority files: `services/package.py`, `services/version.py`, `services/team.py`, `services/dependency.py`
4. Follow existing patterns in `tests/test_*.py`
5. Target: each service file >80% line coverage

## 4. Acceptance Criteria

- Server coverage >80%
- All new tests follow existing patterns
- No flaky tests

## 5. Validation

```bash
cd apps/server && ruff check . && ruff format --check . && pytest --cov=app --cov-fail-under=80
```

## 6. Execution Checklist

- [x] 1. Read coverage report from Task 1.
- [x] 2. Identify gaps.
- [x] 3. Write tests.
- [x] 4. Run validation.
- [ ] 5. Update plan README `[x]`.
- [ ] 6. Update PROGRESS.md.
- [ ] 7. Commit: `git commit -m "test: improve server coverage to >80% - Task 2 of PLAN_phase5_production"`

## 7. Completion & Log

- **Status:** completed
- **Timestamp:** 2026-07-11T11:40:00+08:00
- **Summary of work:**
  - Wrote 10 unit tests for WebhookService: verify_signature (4), _deliver_webhook retry paths (6)
  - Webhook service: 67% → 92%
  - Total server: 80.12% (passes --cov-fail-under=80)
  - fire_webhooks filter logic not unit-tested (covered by test_webhook.py API tests)
- **Files changed:**
  - apps/server/tests/test_webhook_service.py (new, 10 tests)
- **Validation results:**
  - ruff check: PASS
  - ruff format: PASS
  - pytest --cov --cov-fail-under=80: PASS (400 passed)
- **Notes / follow-ups:**
  - Coverage stuck at 80.12% — further gains need storage.py (31%) or team_package.py (66%) tests
  - api/admin.py (83%), api/auth.py (82%), api/versions.py (81%) just below 85%
  - Consider separate task for dedicated coverage improvement push
