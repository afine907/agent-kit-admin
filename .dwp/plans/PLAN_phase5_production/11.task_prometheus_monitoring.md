# Task 11: Prometheus monitoring integration

## 1. Context

No monitoring metrics exposed.

## 2. Goal

Prometheus metrics endpoint at `/metrics`.

## 3. Instructions

1. Add `prometheus-fastapi-instrumentator` to dependencies
2. Create `apps/server/app/middleware/metrics.py`
3. Register in `apps/server/app/main.py`
4. Custom metrics: package_publish_count, package_install_count, webhook_delivery_count
5. Document in `docs/deployment/monitoring.md`

## 4. Acceptance Criteria

- `/metrics` returns Prometheus format
- Standard HTTP metrics tracked
- Custom business metrics tracked
- Documentation complete

## 5. Validation

```bash
cd apps/server && python -c "
from app.main import app
from fastapi.testclient import TestClient
c = TestClient(app)
r = c.get('/metrics')
assert r.status_code == 200
assert 'http_requests' in r.text
"
```

## 6. Execution Checklist

- [ ] 1. Read this task file fully.
- [ ] 2. Add dependency and middleware.
- [ ] 3. Add custom metrics.
- [ ] 4. Document.
- [ ] 5. Update plan README `[x]`.
- [ ] 6. Commit: `git commit -m "feat(server): Prometheus monitoring - Task 11 of PLAN_phase5_production"`

## 7. Completion & Log

- **Status:** pending
- **Timestamp:**
- **Summary of work:**
- **Files changed:**
- **Validation results:**
- **Notes / follow-ups:**
